This is a really trivial project that rolls as many 6 sided die as you specify as its first argument. Really that is all there is to it. 

It is implemented with a for loop that works a bit like how for loops work in C or C++. 
